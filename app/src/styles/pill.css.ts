import { css } from 'lit';

export default css`
  .pill {
    background-color: var(--color-primary-300);
    color: var(--primary-font-color);
    padding: 0.15rem 0.25rem;
    margin: 0.15rem;
    line-height: 1rem;
    border-radius: 5px;
  }
`;
